Subject: Minimal reproducible case - Lasna `service.subscribe(...)` reverts in all tested paths

Hi Reactive/Lasna team,

We reduced the issue to a minimal reproducible case. The remaining blocker is not our business logic, not Base execution wiring, and not smoke-test false positives. The failure is now consistently isolated to Lasna subscription setup itself.

## Minimal conclusion
`service.subscribe(...)` on Lasna reverts with `Failure` in every path we tested:
1. minimal one-subscription probe, constructor-time subscribe
2. minimal one-subscription probe, post-deploy `subscribeOnce(...)`
3. full `VaultSentinelReactive`, constructor-init mode
4. full `VaultSentinelReactive`, post-deploy `initializeDefaultSubscriptions()` mode

## Environment
- Project: `reactive-vault-sentinel`
- Source chain: Sepolia (`11155111`)
- Destination chain: Base Sepolia (`84532`)
- Reactive chain: Lasna (`5318007`)
- Source contract used in repro: `0xFac28E7a698668f160C4735005C34Cbe57EbdC02`
- Topic0 used in repro: `keccak256("AnswerUpdated(int256,uint256,uint256)")`
- Reactive system contract: `0x0000000000000000000000000000000000fffFfF`

## Relevant code
- Minimal probe: `contracts/script/ValidateLasnaOneSub.s.sol`
- Full deploy script: `contracts/script/DeployLasna.s.sol`
- Sentinel contract: `contracts/src/sentinel/VaultSentinelReactive.sol`
- Smoke script: `scripts/e2e/smoke.sh`

## Repro 1 - minimal one-subscription probe
Probe contract:
- `OneSubscriptionProbe._subscribeOnce(...)` calls:
  - `service.subscribe(sourceChainId, sourceContract, topic0, REACTIVE_IGNORE, REACTIVE_IGNORE, REACTIVE_IGNORE)`

Observed result:
- constructor-time subscribe: revert
- post-deploy `subscribeOnce(...)`: revert

Failure shape in trace:
- `0x0000000000000000000000000000000000fffFfF::subscribe(...)`
- then `0x0000000000000000000000000000000000000064::00000000(...)`
- then `Revert: Failure`

This happens even with a single subscription and no app-specific callback logic.

## Repro 2 - full sentinel deploy after Base-side cleanup
We fixed the Base-side execution confounders first:
- `DeployBaseSepolia` now fail-fast requires `VAULT_SENTINEL_REACTIVE`
- `DeployBaseSepolia` supports keystore/account signer flow
- Base was redeployed successfully with:
  - `VaultExecution: 0x00a03196D9536337ed857F5D43dc7aE648e89Bf9`
  - `BasicTransferAdapter: 0x5DCe4Eaaf21Bf4DaCB767dC6a8b45B7165dD92C0`
  - `UniswapStopOrderAdapter: 0x91Da5ee2A393B8056E688BDD43103854b0Ee7128`
  - `AaveProtectionAdapter: 0x06013D3B5cA4662246CC3E345E39BEDf29758484`

After updating env to those new Base addresses, Lasna deploy still failed.

Latest full deploy failure:
- new sentinel address in reverted trace: `0xB0189c0db1aC45cfe1fe8d05f90F3c5d38b09cBf`
- failure occurs in:
  - `VaultSentinelReactive::initializeDefaultSubscriptions()`
  - internal `service.subscribe(...)`
- trace shape:
  - `0x0000000000000000000000000000000000fffFfF::subscribe(...)`
  - `0x0000000000000000000000000000000000000064::00000000(...)`
  - `Revert: Failure`

## Repro 3 - tightened smoke test, no false-positive pass path left
We tightened `scripts/e2e/smoke.sh` so it can no longer pass due to unrelated Base events:
- PASS now requires the current run to observe a new Lasna `Callback`
- Base `ExecutionSucceeded` is checked only after this run's Lasna callback anchor

Latest smoke run after tightening:
- `TX_HASH: 0xf76b9a5ecfc8ed2acd33393aa89b866ce352313255e20ca327e52b0d7d173c60`
- `SEPOLIA_ANSWER_UPDATED_OBSERVED: YES`
- `LASNA_RULE_TRIGGERED_FOUND: NO`
- `LASNA_CALLBACK_EVENTS_FOUND: NO`
- `BASE_EXECUTION_SUCCEEDED_FOUND: NO`
- `SENTINEL_DEBT_DEC: 0`
- `SENTINEL_PAUSED_STATE: unknown`
- `DEFAULT_SUBSCRIPTIONS_INITIALIZED: unknown`
- final diagnosis:
  - `DIAGNOSIS: SOURCE_EVENT_OBSERVED_BUT_NO_REACTIVE_RULETRIGGERED_OR_CALLBACK.`
  - `FAIL: Step11 smoke verification failed.`

This means the old smoke false-positive path is gone. The failure remains.

## What has been ruled out
We have already ruled out these confounders:
- public RPC 403 issue on previous providers
- wrong Git Bash signer path (`/mnt/d/...` vs `/d/...`)
- unpaid Lasna debt (`debt = 0`)
- Base execution zero-address reactive sender misconfiguration
- Base adapter whitelist setup failure
- false-positive smoke PASS caused by unrelated Base execution events
- app-specific business logic as the primary fault domain (minimal single-subscription probe fails too)

## Current strongest fault domain
Lasna raw subscription path itself:
- subscription registration
- ingest/match/dispatch before callback
- or system contract / infra behavior behind `service.subscribe(...)`

## Specific requests
1. Please verify why `service.subscribe(...)` is reverting on Lasna for this minimal case.
2. Please inspect the internal failure behind:
   - `0x0000000000000000000000000000000000fffFfF::subscribe(...)`
   - forwarded to `0x0000000000000000000000000000000000000064::00000000(...)`
3. Please confirm whether this is:
   - unsupported subscription path
   - infra/indexer issue
   - chain-side configuration issue
   - callback registration policy issue
   - gas/deposit requirement not surfaced to callers
4. Please confirm whether `eth_getLogs` / callback visibility on Lasna is degraded for this deployment window.

## Local artifacts
- `scripts/e2e/step11-incident-evidence.txt`
- `scripts/e2e/step11-escalation-final.txt`
- `contracts/script/ValidateLasnaOneSub.s.sol`
- `contracts/script/DeployLasna.s.sol`
- `scripts/e2e/smoke.sh`

If needed, we can provide the exact forge commands and full traces, but the key point is simple:

> even the smallest possible single-subscription repro fails at Lasna `service.subscribe(...)` before any useful app logic runs.
